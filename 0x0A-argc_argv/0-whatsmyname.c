#include <stdio.h>

/**
  * main - entry
  * @argc: num of args
  * @argv: array of args
  * Return: return 0
  */

int main (int argc __attribute__((unused)), char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
